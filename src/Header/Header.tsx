import { Flex, Text } from '@chakra-ui/react';

const Header = () => {
    return (
        <Flex w="100%" boxShadow={'base'} p='3.5' bgColor={'white'}>
            <Text>Heaer</Text>
        </Flex>
    );
};

export default Header;
